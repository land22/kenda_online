{% extends 'baseAdmin.html.twig' %}


{% block body %}
    <h1>liste des catégories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ ken_category.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ ken_category.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ken_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ken_categories_index') }}">Retour à la liste</a>

    <a href="{{ path('ken_categories_edit', {'id': ken_category.id}) }}">modifier</a>

    {{ include('ken_categories/_delete_form.html.twig') }}
{% endblock %}
