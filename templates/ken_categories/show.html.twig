{% extends 'base.html.twig' %}

{% block title %}KenCategories{% endblock %}

{% block body %}
    <h1>KenCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ken_category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ken_category.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ ken_category.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ken_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ken_categories_index') }}">back to list</a>

    <a href="{{ path('ken_categories_edit', {'id': ken_category.id}) }}">edit</a>

    {{ include('ken_categories/_delete_form.html.twig') }}
{% endblock %}
